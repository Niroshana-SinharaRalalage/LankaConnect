namespace LankaConnect.SharedKernel.Contracts;

/// <summary>
/// W1D SKELETON marker — anchors NetArchTest <c>Types.InAssembly()</c> calls
/// until SharedKernel-level integration events land (Wave 2 cultural events,
/// W1F localization events).
/// </summary>
public static class AssemblyMarker
{
}
