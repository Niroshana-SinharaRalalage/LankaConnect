namespace LankaConnect.SharedKernel.Money;

/// <summary>
/// W1D SKELETON marker — anchors NetArchTest <c>Types.InAssembly()</c> calls
/// until W1E moves Money + Currency from BuildingBlocks.Domain (per D6).
/// </summary>
public static class AssemblyMarker
{
}
