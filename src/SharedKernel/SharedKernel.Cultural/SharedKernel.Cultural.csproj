<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>LankaConnect.SharedKernel.Cultural</RootNamespace>
    <AssemblyName>LankaConnect.SharedKernel.Cultural</AssemblyName>
    <Description>SharedKernel.Cultural — LankaConnect-specific cross-domain cultural primitives. CulturalContext, SriLankanLanguage, GeographicRegion, ReligiousContext, CulturalBackground, CulturalProfile, ICulturalCalendar (interface). Per ADR-006 / ADR-008: depends only on BuildingBlocks.*; consumed by every Capability + Product that needs cultural awareness. Unblocks W4.1 Communications extraction.</Description>
  </PropertyGroup>

  <!-- W1D SKELETON (2026-06-04). Empty shell — cultural types move from LankaConnect.Domain in Wave 2. -->

  <ItemGroup>
    <ProjectReference Include="..\..\BuildingBlocks\BuildingBlocks.Domain\BuildingBlocks.Domain.csproj" />
    <PackageReference Include="Microsoft.Extensions.Caching.Memory" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

</Project>
