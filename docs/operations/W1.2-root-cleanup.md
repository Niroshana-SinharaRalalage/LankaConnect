# W1.2 — Root-Level Debug Artifact Cleanup

| | |
|---|---|
| **Date** | 2026-05-11 |
| **PR branch** | `refactor/phase-a/w1.2-root-debug-cleanup` |
| **Scope** | Delete 234 tracked debug artifacts from repo root + extend `.gitignore` |
| **Out of scope** | `scripts/` folder cleanup (W1.3); test project triage (W1.0b done); secret rotation (W1.1 deferred) |

## What was deleted (234 files)

All files at repo root NOT matching the legitimate-project whitelist (28 files kept). Categories:

| Category | Examples | Count |
|---|---|---|
| Build output captures | `build_baseline.txt`, `build_priority1.txt`, `new_build.txt` | ~50 |
| Test scripts | `test-create-event-with-email-groups.ps1`, `test_phase6a81.json` | ~70 |
| API response / payload captures | `api-result.json`, `events_list.json`, `register_response.json` | ~40 |
| Hidden debug captures | `.event_forms.json`, `.my_events.json`, `.update_payload.json` | ~11 |
| Diagnostic / check scripts | `check-badge-nulls.sql`, `diagnose-badge-nulls.ps1`, `query-aaaa-commitments.sql` | ~20 |
| Build error / type analysis dumps | `all-errors.txt`, `error_patterns.txt`, `missing_types.txt`, `interface-gaps.txt` | ~20 |
| Windows-path-encoded filenames (debug script bugs) | `C:WorkLankaConnectbuild_result.txt` etc. | 13 |

## What was kept (28 files — the legitimate-project whitelist)

```
.editorconfig          AZURE_STAGING_CREDENTIALS.md      Directory.Build.props
.env.docker            AlertSeverityConsolidationValidation.cs   Directory.Packages.props
.gitignore             CLAUDE.md                         FIXES_NEEDED.md
                       DIAGNOSIS_REPORT.md               LankaConnect.sln
otherprompts2          DUAL_PRICING_INVESTIGATION_REPORT.md      MANUAL_CHANGES_DETECTION_GUIDE.md
otherprompts3          NEWSLETTER_METRO_MATCHING_SOLUTION.md     MANUAL_CHANGES_SOLUTION.md
                       NEXT_STEPS.md                     PHASE_6A_34_RESPONSE.md
package.json           QUICK_REFERENCE.md                QUICK_START_RCA_TEST.md
package-lock.json      TestExecutionReport.md            VERIFY_MIGRATION.md
claude-flow.config.json
docker-compose.yml
progress-state.json
```

## `.gitignore` patterns added

Comprehensive glob blocks at root level only (`/<pattern>`) for: build output captures, test scripts, API response/payload captures, hidden debug captures, diagnostic scripts, build error dumps, and stray Windows-path-encoded filenames. See `.gitignore` lines after "Root-level debug artifacts (W1.2 cleanup 2026-05-11)" header.

## Items NOT touched in W1.2 (deferred)

- `AlertSeverityConsolidationValidation.cs` — orphan source file at repo root; unsure if intended; left in place pending architect review
- `.env.docker` — config file; not verified contents in this PR; left in place
- `otherprompts2`, `otherprompts3` — folder names suggesting prompt experiments; left in place
- `scripts/` folder cleanup — separate task W1.3
- `tests/` folder cleanup — already triaged in W1.0b

## Verification

```bash
# 1. Only 28 root files remain (matches whitelist)
git ls-files --full-name -- . | grep -v "/" | wc -l
# Expected: 28

# 2. No debug-pattern files re-introducible
echo "test" > test-foo.ps1 && git status --short test-foo.ps1
# Expected: empty (ignored); cleanup: rm test-foo.ps1
```
