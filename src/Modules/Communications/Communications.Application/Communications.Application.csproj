<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>LankaConnect.Modules.Communications.Application</RootNamespace>
    <AssemblyName>LankaConnect.Modules.Communications.Application</AssemblyName>
    <Description>Communications module â€” Application layer. MediatR command/query handlers, DTOs, application services. Depends on Domain + Contracts + BuildingBlocks.Application.</Description>
  </PropertyGroup>

  <!-- W4 SKELETON (2026-06-04). Empty shell â€” handlers move in W4.1.4 per W3.9 playbook Â§Step 4. -->

  <ItemGroup>
    <ProjectReference Include="..\Communications.Domain\Communications.Domain.csproj" />
    <ProjectReference Include="..\Communications.Contracts\Communications.Contracts.csproj" />
    <ProjectReference Include="..\..\..\BuildingBlocks\BuildingBlocks.Application\BuildingBlocks.Application.csproj" />
    <!-- Wave 6.5.f mirror (2026-07-09 Day 4): clean-direction PR to
         Communications.Infrastructure mirroring Forms/LankaEvents Application â†’
         Infrastructure. Enabled by cycle-break commit that dropped the reverse
         edge (INewsletterRecipientService + RecipientPreviewDto promoted to
         Contracts.LegacyPromotions). Handlers now inject CommunicationsDbContext
         directly. -->
    <ProjectReference Include="..\Communications.Infrastructure\Communications.Infrastructure.csproj" />
    <!-- 4C.h Day 5 (2026-07-10): GetPublishedNewslettersQueryHandler needs
         LankaEventsDbContext.MetroAreas (LankaEvents-owned; newsletter has
         location-based targeting via MetroArea associations). -->
    <ProjectReference Include="..\..\..\Products\LankaEvents\LankaEvents.Infrastructure\LankaEvents.Infrastructure.csproj" />
    <!--
      W5.4.b TRANSITIONAL EDGE (2026-06-13). EmailGroupQueries wraps the
      existing IEmailGroupRepository which still lives in LankaConnect.Domain
      until W5.4.d.2 (physical move). Edge mirrors the Forms 5.3c.0 transitional
      Forms.Application -> LankaConnect.Application pattern (different layer
      here because IEmailGroupRepository sits in LankaConnect.Domain, not
      Application). Cut alongside the EmailGroup file move in W5.4.d.2.
    -->
    <!--
      W5.4.c.1 TRANSITIONAL EDGE (2026-06-22). The 5 moved command handlers
      (CreateEmailGroup / UpdateEmailGroup / DeleteEmailGroup / CreateNewsletter
      / UpdateNewsletter) depend on ICommand / ICommandHandler /
      ICurrentUserService / IUnitOfWork that still live in
      LankaConnect.Application until BuildingBlocks.Application owns those
      primitives. Mirrors the Forms 5.3c.0 + Notifications W3.4 transitional
      edges with the matching ArchTest relaxation. Cut alongside the
      BuildingBlocks.Application elevation pass.
    -->
    <ProjectReference Include="..\..\..\LankaConnect.Application\LankaConnect.Application.csproj" />
    <ProjectReference Include="..\..\Identity\Identity.Contracts\Identity.Contracts.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="MediatR" />
    <PackageReference Include="FluentValidation" />
  </ItemGroup>

</Project>
