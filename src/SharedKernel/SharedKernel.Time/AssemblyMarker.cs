namespace LankaConnect.SharedKernel.Time;

/// <summary>
/// W1D SKELETON marker — anchors NetArchTest <c>Types.InAssembly()</c> calls
/// until W1G + Wave 4 land time primitives (DateRange, ITimeZoneService).
/// IClock lives in BuildingBlocks.Abstractions; SharedKernel.Time owns
/// LankaConnect-specific time semantics only.
/// </summary>
public static class AssemblyMarker
{
}
