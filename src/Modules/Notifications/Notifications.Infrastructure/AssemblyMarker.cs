namespace LankaConnect.Modules.Notifications.Infrastructure;

/// <summary>
/// W3.1 SKELETON marker — anchors NetArchTest <c>Types.InAssembly()</c> calls
/// until W3.4 / W3.5 fill the layer with NotificationsDbContext +
/// NotificationConfiguration + NotificationRepository + the baseline
/// migration for the <c>notifications</c> schema.
/// </summary>
public static class AssemblyMarker
{
}
