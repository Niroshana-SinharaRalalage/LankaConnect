<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>LankaConnect.Modules.Notifications.Infrastructure</RootNamespace>
    <AssemblyName>LankaConnect.Modules.Notifications.Infrastructure</AssemblyName>
    <Description>Notifications module — Infrastructure layer. NotificationsDbContext (notifications schema), EF Core configurations, repositories, integration-event dispatch impl. Depends on Notifications.Application + Notifications.Domain + Notifications.Contracts + BuildingBlocks.Infrastructure (for BaseDbContext + JSONB helpers). No Web / Modules.* leakage.</Description>
  </PropertyGroup>

  <!--
    W3.1 SKELETON (2026-06-02). Empty shell — DbContext + repositories + baseline migration land in W3.4 / W3.5.
  -->

  <ItemGroup>
    <ProjectReference Include="..\Notifications.Application\Notifications.Application.csproj" />
    <ProjectReference Include="..\Notifications.Domain\Notifications.Domain.csproj" />
    <ProjectReference Include="..\Notifications.Contracts\Notifications.Contracts.csproj" />
    <ProjectReference Include="..\..\..\BuildingBlocks\BuildingBlocks.Infrastructure\BuildingBlocks.Infrastructure.csproj" />
    <!--
      W3.4 TRANSITIONAL EDGE (2026-06-03). Notifications.Infrastructure references
      the legacy LankaConnect.Infrastructure for Repository<T> base class until
      that primitive moves to BuildingBlocks.Infrastructure. Cut in a follow-up
      alongside the BuildingBlocks elevation pass.
    -->
    <ProjectReference Include="..\..\..\LankaConnect.Infrastructure\LankaConnect.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Relational" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
  </ItemGroup>

</Project>
