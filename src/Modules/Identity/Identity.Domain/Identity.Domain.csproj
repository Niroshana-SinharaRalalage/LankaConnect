<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>LankaConnect.Modules.Identity.Domain</RootNamespace>
    <AssemblyName>LankaConnect.Modules.Identity.Domain</AssemblyName>
    <Description>Identity module domain layer. Owns the User aggregate (User entity, IUserRepository, value objects, enums, domain events). Wave 4.7.e (2026-06-26) physical extraction from LankaConnect.Domain.Users per architect Option A ruling: 14 legacy LankaConnect.Application consumers continue to inject IUserRepository via an allow-listed ArchTest exception that shrinks to zero during Cross-cutting cleanup.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Identity.Contracts\Identity.Contracts.csproj" />
    <!-- Wave 8.5-cleanup (2026-07-18) ExtractabilityAudit GAP-6: Email + PhoneNumber
         promoted from Products.LankaEvents.Domain.ValueObjects to SharedKernel.Contact.
         User.Email + User.PhoneNumber are the highest-value platform primitives; landing
         them here removes the Products→SharedKernel inversion at User's most-central prop.
         The LankaEvents.Domain PR below still exists for MetroArea reference-data typing;
         that PR drops when MetroArea also promotes out of Products (separate follow-up). -->
    <ProjectReference Include="..\..\..\SharedKernel\SharedKernel.Contact\SharedKernel.Contact.csproj" />
    <ProjectReference Include="..\..\..\Products\LankaEvents\LankaEvents.Domain\LankaEvents.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Wave 4.7.e: UserRepository in Identity.Infrastructure calls
         internal User.SyncPreferredMetroAreaIdsFromEntities(). Same boundary
         relationship the legacy LankaConnect.Domain â†’ LankaConnect.Infrastructure
         had pre-extraction. -->
    <InternalsVisibleTo Include="LankaConnect.Modules.Identity.Infrastructure" />
  </ItemGroup>

</Project>
