<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <RootNamespace>LankaConnect.BuildingBlocks.Domain.Tests</RootNamespace>
    <AssemblyName>LankaConnect.BuildingBlocks.Domain.Tests</AssemblyName>
    <Description>Unit tests for BuildingBlocks.Domain foundation primitives (Result, Maybe, Entity, ValueObject, BusinessRule, Guard) and value objects (Money, Currency, Locale, Country). Per master TODO §W2.3 acceptance: 90%+ coverage on each type.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="FluentAssertions" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
    <Using Include="FluentAssertions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\BuildingBlocks\BuildingBlocks.Domain\BuildingBlocks.Domain.csproj" />
    <!-- W1E: Money + Currency tests live here; types moved to SharedKernel.Money per D6. -->
    <ProjectReference Include="..\..\src\SharedKernel\SharedKernel.Money\SharedKernel.Money.csproj" />
    <!-- W1F: Locale + Country tests live here; types moved to SharedKernel.Locale per ADR-001. -->
    <ProjectReference Include="..\..\src\SharedKernel\SharedKernel.Locale\SharedKernel.Locale.csproj" />
  </ItemGroup>

</Project>
