namespace LankaConnect.BuildingBlocks.Domain;

/// <summary>
/// W2.1 SKELETON marker — anchors NetArchTest <c>Types.InAssembly()</c> calls until
/// W2.3 fills this assembly with foundation types (Result&lt;T&gt;, Money, etc.).
/// </summary>
/// <remarks>
/// Internal so it doesn't leak into the public API. Remove or replace once
/// real types live in this assembly; until then it's the only type and
/// architecture tests use it to locate the assembly.
/// </remarks>
public static class AssemblyMarker
{
}
