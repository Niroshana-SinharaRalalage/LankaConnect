namespace LankaConnect.SharedKernel.Identity;

/// <summary>
/// W1D SKELETON marker — anchors NetArchTest <c>Types.InAssembly()</c> calls
/// until W1G adds typed-ID value objects (UserId, TenantId, StorefrontId,
/// OrganizationId) + IUserContext per ADR-006 D7.
/// </summary>
public static class AssemblyMarker
{
}
