namespace LankaConnect.Products.LankaTemples.Domain;

/// <summary>
/// Anchor type for reflection-based assembly discovery (MediatR, AutoMapper, ArchTest).
/// Do not delete — presence in the assembly manifest is required.
/// Phase B scaffolding per Consult #27 Q5 (2026-07-15).
/// </summary>
public sealed class AssemblyMarker
{
    private AssemblyMarker() { }
}
